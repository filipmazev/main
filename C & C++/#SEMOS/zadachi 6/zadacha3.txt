#include <iostream>
#include <fstream>
using namespace std;
int main()
{
    ofstream wright("shah.txt");/* deklarirame datoteka
    vo koja kje pishuvame */
    char symbol, symbol2; /*deklarirame promenlivi
    od tipot char, za korisnikot da mozhe da vnese shto
    saka da ima vo nizata */
    cin>>symbol>>symbol2; //vnesuvanje na tie promenlivi
    int n=5;//promenliva int, za golemina na nizata

    /*dodeka promenlivite 'i' i 'j' ne stignat do n odnosno
    do 5, ova e za goleminata a linijata, koga kje sitgne 
    do 5, da odi na sledno */
    //cel ovoj del e za da se namestat na soodvetnite pozocii karkaterite 
    for(int i=0; i<=n; i++) //da broi od i nagore dodeka ne stigne do n
        {
            for(int j=0; j<=n; j++)//da broi od j nagore dodeka ne stigne do j
            {
                /* vo zavisnost od toa dali e parno ili neparno postavuva
                soodveten simbol na taa pozicija, primer ako i koja 
                odi od 0 do 5, ima sluchaevi kade e parna i neparna,
                vo parnite pozicii na 'i' i na 'j' promenlivata se mesti 
                prviot simbol,istoto vazhi i za sluchajot koga verziite
                na ovie promenlivi se neparni, ova bi bilo za drugiot 
                simbol, koj go stava vo mestoto na neparnite */
                if(i%2==0 && j%2==0 || i%2!=0 && j%2!=0 )
                wright<<symbol;/*sluchajot koga se parni i dvata da 
                go ispechati prviot simbbol */
                else
                //vo vtoriot slucha koga se neparni
                wright<<symbol2;//se pechati vtoriot simbol
            }
        wright<<endl;
        }
    wright.close();
return 0;
}